apiVersion: v1
kind: Service
metadata:
  name: simulador-chaos
spec:
  selector:
    app: simulador-chaos
  ports:
  - port: 3001
    targetPort: 3000
    name: http
    protocol: TCP
  type: LoadBalancer
