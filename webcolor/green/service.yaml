apiVersion: v1
kind: Service
metadata:
  name: webcolor-green
spec:
  selector:
    app: webcolor
    version: green
  ports:
  - port: 8002
    targetPort: 80
    name: http
    protocol: TCP
  type: LoadBalancer
